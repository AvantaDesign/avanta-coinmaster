{
  "name": "avanta-coinmaster",
  "version": "1.0.0",
  "description": "Sistema de contabilidad personal y empresarial para persona física con actividad empresarial en México",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:frontend": "vite",
    "dev:full": "npm run build && wrangler pages dev dist --d1 DB=avanta-coinmaster --r2 RECEIPTS=avanta-receipts --port 8788",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist",
    "deploy:preview": "npm run build && wrangler pages deploy dist --env preview",
    "db:health": "curl http://127.0.0.1:8788/api/health",
    "db:check": "wrangler d1 execute avanta-coinmaster --command=\"SELECT COUNT(*) as table_count FROM sqlite_master WHERE type='table'\"",
    "db:list": "wrangler d1 execute avanta-coinmaster --command=\"SELECT name FROM sqlite_master WHERE type='table' ORDER BY name\"",
    "db:views": "wrangler d1 execute avanta-coinmaster --command=\"SELECT name FROM sqlite_master WHERE type='view' ORDER BY name\"",
    "test:api": "curl https://avanta-coinmaster.pages.dev/api/health",
    "test:local": "npm run dev:full & sleep 5 && npm run db:health"
  },
  "dependencies": {
    "@heroicons/react": "^2.2.0",
    "@tanstack/react-virtual": "^3.13.12",
    "decimal.js": "^10.6.0",
    "jose": "^6.1.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.0",
    "tesseract.js": "^6.0.1",
    "zustand": "^4.5.7"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "tailwindcss": "^3.3.6",
    "vite": "^5.0.8"
  }
}
